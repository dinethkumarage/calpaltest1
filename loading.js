document.addEventListener('DOMContentLoaded', () => {
    // Simulate loading time
    setTimeout(() => {
        window.location.href = 'home.html'; // Redirect to home page after loading
    }, 3000); // 3 second loading time
});